* 자바빈 (java bean)  => 데이터 모음 객체

- string 객체 20개를 보내는 것 보다, string 객체 20개를 모아서 한번에 보내는 것이 효율적이다. 
- 리스트로 보내고, 리스트로 받아와서 반복문을 통해 처리 한다. 

- 자바빈이란 java언어의 데이터(변수)와 기능 (메서드)으로 이루어진 클래스입니다.

- 자바빈은 데이터를 저장하는 변수, 데이터를 읽어오는 메서드(getter), 데이터를 저장할 때 사용되는 메서드(setter)로 이루어져 있습니다.

- 자바빈은 데이터 베이스와의 반복적인 작업을 효율적으로 처리하기 위해 사용합니다.

- jsp에서는 액션태그를 사용하여 자바빈을 사용할 수 있는 방법이 있습니다.

- 은닉을 통해 설계 
